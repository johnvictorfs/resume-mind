{
  "name": "resume-mind",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/web",
    "packages/server",
    "packages/ai",
    "packages/schemas"
  ],
  "scripts": {
    "build": "bun -F '*' build",
    "check": "bun -F '*' check",
    "check:unsafe": "bun -F '*' check:unsafe",
    "check:write": "bun -F '*' check:write",
    "db:generate": "bun -F '*' db:generate",
    "db:migrate": "bun -F '*' db:migrate",
    "db:push": "bun -F '*' db:push",
    "db:studio": "bun -F '*' db:studio",
    "dev": "bun -F '*' dev",
    "typecheck": "bun -F '*' typecheck"
  },
  "ct3aMetadata": {
    "initVersion": "7.40.0"
  }
}
